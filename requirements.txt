fastapi[all]
requests

aiohttp

pypdf
rank_bm25

chromadb

# --- LangChain 1.x stack (required by deepagents) ---
langchain-core>=1.4.7,<2
langchain>=1.3.9,<2
langsmith>=0.8.11

langchain-openai>=1.3.0,<2
langchain-ollama>=1.1.0,<2
langchain-community>=0.4.2,<0.5
langchain-chroma>=1.1.0,<2
langchain-huggingface>=1.2.2,<2
langchain-mongodb>=0.11.0

langgraph>=1.2.0,<2
langgraph-checkpoint-mongodb>=0.4.0
langgraph-store-mongodb>=0.3.0

# Tracing/observability (self-hosted). v4 = OTEL-native: langfuse.langchain.CallbackHandler
# + @observe + get_client. Tracing is opt-in via LANGFUSE_* env; absent keys → no-op.
langfuse>=4,<5

deepagents>=0.6.11,<0.7


websockets>=15.0,<16.0

python-dotenv>=1.0.0
feedparser
bs4

retry
python-socketio[asyncio-client]>=5.0.0

# Web/OSINT recon (optional, best-effort): DuckDuckGo search + HTML-to-text.
# The recon toolkit also auto-detects CAMEL (`camel-ai`) if installed.
ddgs
html2text

# transformers==4.46.*
# accelerate==1.0.*
# bitsandbytes==0.44.*
